{
  "name": "zevent-ws-server",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal TypeScript WebSocket + HTTP server for routing messages to OBS docks by streamer id.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "tsx watch src/index.ts"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "puppeteer": "21.3.8",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/ws": "^8.18.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
